# Resend API key used by the Vercel `/api/contact` route.
RESEND_API_KEY="re_123"

# Optional custom sender address for production domains.
# Defaults to `Chicago Elite Painting <onboarding@resend.dev>` when omitted.
RESEND_FROM_EMAIL="Chicago Elite Painting <hello@yourdomain.com>"
